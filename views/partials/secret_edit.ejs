<div class="secret-edit">
  <form
    hx-put="/services/<%= serviceName %>/secrets/<%= secretName %>"
    hx-target="#secret-value-<%= serviceName %>-<%= secretName %>"
    hx-swap="innerHTML"
  >
    <textarea name="secretValue" class="secret-edit-input" rows="5" required><%= secretValue %></textarea>
    <div class="secret-actions">
      <button type="submit" class="btn btn-small btn-primary">💾 Save</button>
      <button
        type="button"
        class="btn btn-small btn-secondary"
        hx-get="/services/<%= serviceName %>/secrets/<%= secretName %>"
        hx-target="#secret-value-<%= serviceName %>-<%= secretName %>"
        hx-swap="innerHTML"
      >
        Cancel
      </button>
    </div>
  </form>
</div>

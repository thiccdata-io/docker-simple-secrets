<div class="alert alert-success">✓ <%= keyMessage %></div>

<script>
  // Load services section after successful authentication
  htmx.ajax('GET', '/services/view', {
    target: '#services-container',
    swap: 'innerHTML'
  });

  <% if (autoDeployPassword) { %>
  // Auto-deploy on first setup
  setTimeout(function() {
    // Wait for services to load first
    setTimeout(function() {
      const deployBtn = document.getElementById('deploy-button');
      if (deployBtn) {
        deployBtn.click();
      }
    }, 500);
  }, 100);
  <% } %>
</script>
